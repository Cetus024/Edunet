# EduNets 最近三小时整合工作总结

整理时间：2026-09-05 20:57（新加坡时间）
整合窗口：约 17:57–20:57。内容包含本窗口完成的整合与验证，以及此前已写好、此次统一提交的功能；不把下午较早的 Git 提交算作本窗口新提交。

## 本次完成

### Rescue Room：共同题目、各自手写

采用用户最终选择的玩法：大家看同一道系统题目，各自在白板上写解题过程，经过 OCR 和人工确认后交给 AI 分析。这不是经典猜词版 skribbl。

- 替换原来的选择按钮和 45 秒抢答界面，保留小队、加入房间、邀请、在线状态、三轮练习和完成记录。
- 支持鼠标、触屏和触控笔；提供黑／蓝／红笔、橡皮、撤销和清空。
- 服务器以房间题目为准，不接受客户端替换题目或提交到已结束的轮次。
- 个人原稿和反馈在练习时只对本人可见，结束后可查看小队回顾。
- 在线成员提交后，主持人主动切题；离线且未提交的成员可跳过，避免房间永久卡住。
- 支持修改后重新分析，不以 AI 意见自动计算正式分数。
- 原选择题提交接口明确停用，避免绕过新流程。

### Revision Room：手写 → OCR → 确认 → 分析

- 将主流程从麦克风讲解改为手写解题。
- 学生输入原题、已知条件、单位及必要的图形关系。
- OCR 后可修改识别文字；必须确认文字准确后才可分析。
- 直接分析原始解题步骤，不先摘要，避免丢失公式、单位和推导。
- 分开显示步骤问题、概念冲突、无法判断的部分和可采取的下一步。
- 保存手写原稿、确认后的文字、题目和分析；仍可查看旧语音记录。

### Capture Hub 与 app 整合

- 保留既有多文件拖放上传和已提交的无障碍笔记阅读器。
- 整合手机大图自动压缩：接受不超过 20 MiB 的 PNG／JPEG／WebP，压缩后的图片不超过 3 MiB，并对齐 API 校验。
- 增加压缩进度、压缩前后大小和过大请求的提示。
- 整合可折叠的识别文字复核区和快速跳转。
- 更新 Study Squad 入口图标、Rescue 加入界面、相关中英文文案及吉祥物提示。

### 后端与数据

- 新增 `edunets.learning_work` 表及迁移 `0017_tearful_butterfly.sql`。
- 迁移已执行，并通过运行角色和实际数据库操作验证。
- 加入成员权限、提交内容与笔画大小校验、个人答案可见性、轮次／重开校验及提交重试处理。
- 为详细解题反馈增加单独的模型输出预算和超时配置，不改变原短摘要默认值。
- 模型引用必须出现在提交文字中；无课程参考资料或识别有歧义时，不能显示为确认正确。
- 模型失败或繁忙时显示错误并保留草稿，不生成虚构的成功结果。

## 验证结果

- TypeScript：前端和 API 类型检查通过。
- 自动测试：26 个测试文件、201 个测试全部通过。
- Lint：整站无错误；教师 dashboard 有一条既有的 `useMemo` 依赖警告。本次修改文件的定向 lint 通过。
- 构建：最终整站静态页面与 API 构建通过，生成 26 个静态页面。
- 浏览器：桌面／390px 手机宽度、中英文切换、画布输入、OCR 回填、确认后提交、失败保留与重试、Rescue 修改重试均已检查；手机页面未发生横向溢出。
- Capture Hub：同时选取两张图片完成处理；一张 15.6 MiB 测试 PNG 被压缩到 2.9 MiB，Base64 长度 4,002,784，低于 API 上限。
- 真实服务：一条合成代数题通过 Azure OCR 和实际 AI 分析，识别出算式并返回两项步骤反馈和下一步选项。
- 多人流程：使用真实数据库及确定性的模型测试响应，验证服务器题目、重复与并发提交、修改后重提、答案隔离、主持人权限、在线成员等待、旧轮次拒绝及 Revision 结束流程。临时测试账号和级联记录已删除。

浏览器交互测试使用本地测试响应；真实 OCR／AI 连通性和真实数据库流程分别验证，不将它们误称为完整真实多人模型端到端测试。

## 已知限制与选择

1. 当前分析读取确认后的文字，不能直接理解几何图、箭头或二维公式布局。需要补充角度、长度、关系、分数与指数；若希望直接理解图形，可另做视觉模型输入方案。
2. AI 反馈用于学习检查，不是符号计算器或正式阅卷。无法可靠判断时提供补条件、修正识别、重新分析或向老师确认的选项。
3. 连续真实模型调用在验证中曾出现失败；单条真实检查成功，失败时会保留草稿并支持重试。多人高并发仍受当前模型服务容量影响。
4. 本次完成源码整合、数据库迁移和验证；线上前端是否更新以 Git 推送后的部署状态为准。

## 此前已提交并保留的成果

- `d3f2412`，2026-09-05 15:18：Capture Hub 拖放与多文件 OCR 上传。
- `4ebf69a`，2026-09-05 15:46：Capture Hub 无障碍笔记阅读器。

这些提交早于上述三小时窗口，列在此处仅说明本次整合的基础。
